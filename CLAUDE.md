# BoldSearch — Claude Code Notes

@AGENTS.md

The import above loads the repository agent guidance (structure, setup, verification, runtime data, evaluation constraints, and Git conventions) so Claude follows the same rules as every other agent. Do not duplicate that content here.
